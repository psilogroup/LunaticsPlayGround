varying vec3 N;
varying vec3 v;

uniform mat4 u_model;
uniform mat4 u_view;
uniform mat4 u_projection;

void main(void)
{

   mat4 modelView = u_projection * u_view * u_model * gl_Vertex
   v = vec3(gl_ModelViewMatrix * gl_Vertex);       
   N = normalize(gl_NormalMatrix * gl_Normal);
   gl_Position = v;


}
